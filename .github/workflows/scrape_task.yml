name: PubMed Continuous Scraper

on:
  workflow_dispatch:
    inputs:
      search_query:
        description: 'PubMed Search Query'
        required: false
  repository_dispatch:
    types: [next_batch]
  schedule:
    - cron: '*/20 * * * *' # Backup every 20 minutes

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Necessary for rebase

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install biopython

      - name: Run Scraper
        env:
          INPUT_QUERY: ${{ github.event.inputs.search_query }}
        run: |
          if [ ! -z "$INPUT_QUERY" ]; then
            echo "$INPUT_QUERY" > last_query.txt
            echo "0" > checkpoint.txt
            rm -f leads.csv
          fi
          python scraper.py > output.log
          cat output.log
          if grep -q "FINISHED" output.log; then
            echo "STATUS=DONE" >> $GITHUB_ENV
          else
            echo "STATUS=CONTINUE" >> $GITHUB_ENV
          fi

      - name: Update README Dashboard
        run: |
          if [ -f leads.csv ]; then
            COUNT=$(($(wc -l < leads.csv) - 1))
            sed -i "s/\*\*Total Unique Leads:\*\* [0-9]*/\*\*Total Unique Leads:\*\* $COUNT/" README.md
          fi

      - name: Commit and Push (Safe Rebase)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git pull --rebase origin main
          git commit -m "Update Leads [skip ci]" || exit 0
          git push

      - name: Auto-Trigger Next Batch
        if: env.STATUS == 'CONTINUE'
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          event-type: next_batch
